version: "3.9"

services:
  postgres:
    image: postgres:14.1
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: "shop"
      POSTGRES_USER: "shopuser"
      POSTGRES_PASSWORD: "520911"
    ports:
      - "5433:5432"

  mailhog:
    image: mailhog/mailhog:latest
    restart: always
    ports:
      - "1025:1025"
      - "8025:8025"

  redis:
    image: 'redis:alpine'
    ports:
      - "6379:6379"

#  api:
#    depends_on:
#      - postgres
#      - mailhog
#      - redis
#    build: .
#    command:
#      - python3 ./shop/manage.py runserver 0.0.0.0:8000
#      - celery -A shop worker -l info
#    ports:
#      - "8000:8000"
#
#  nginx:
#    build: ./nginx
#    depends_on:
#      - api
#    ports:
#      - "8080:80"

volumes:
  postgres_data:
